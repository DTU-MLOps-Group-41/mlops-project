batch_size: 64
learning_rate: 1e-3
weight_decay: 0.01
num_epochs: 3
precision: 16-mixed
deterministic: false
log_every_n_steps: 10
patience: 3

# Training metadata
name: baseline
description: "Default baseline training configuration for quick experiments"
