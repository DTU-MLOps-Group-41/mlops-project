# Baseline experiment - uses defaults
defaults:
  - override /training: baseline
  - override /dataset: small
  - _self_

# Global settings

seed: 42

# Experiment metadata
experiment:
  name: baseline
  description: "Baseline experiment with default hyperparameters"
